Nothing should be ignored based on the provided file changes. The added files are all documentation and batch script files (.md, .bat) which are source/config files that should be tracked in version control.